{
  "name": "jgmarry.me",
  "version": "0.1.0",
  "homepage": "https://jgmarry.me",
  "private": true,
  "dependencies": {
    "dotenv": "^16.0.3",
    "firebase": "^9.13.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^5.0.0",
    "react-scripts": "5.0.1",
    "string-sanitizer": "^2.0.2",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "predeploy": "npm run build:production",
    "deploy": "gh-pages -d build:production",
    "start": "react-scripts start",
    "build": ". .env.${REACT_APP_ENV} && react-scripts build",
    "build:development": "set REACT_APP_ENV=development && npm run build",
    "build:production": "set REACT_APP_ENV=production && npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "assert": "^2.0.0",
    "buffer": "^6.0.3",
    "crypto-browserify": "^3.12.0",
    "gh-pages": "^4.0.0",
    "http-status-codes": "^2.2.0",
    "https-browserify": "^1.0.0",
    "os-browserify": "^0.3.0",
    "process": "^0.11.10",
    "react-app-rewired": "^2.2.1",
    "react-router-dom": "^6.8.0",
    "react-select": "^5.7.0",
    "stream-browserify": "^3.0.0",
    "stream-http": "^3.2.0",
    "url": "^0.11.0"
  }
}
